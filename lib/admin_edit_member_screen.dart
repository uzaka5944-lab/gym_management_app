// lib/admin_edit_member_screen.dart
import 'package:flutter/material.dart';

class AdminEditMemberScreen extends StatelessWidget {
  const AdminEditMemberScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Edit Member'),
      ),
      body: const Center(
        child: Text('Edit Member form coming soon!'),
      ),
    );
  }
}